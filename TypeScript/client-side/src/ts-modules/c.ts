// export default 'li'

// export = 和 import xx = require()
/**
 * es6：export default 和 nodejs：module.exports
 * 均可以默认导出
 * 但这2种形式不兼容
 * ts为解决这种问题增加 export = 和 import xx = require()
 */

const name = 'li'
export = name


